# Shell scripts must always use LF line endings, even on Windows.
# CRLF breaks `exec` inside Linux containers: the shebang becomes
# "#!/bin/bash\r" which fails with "no such file or directory".
*.sh text eol=lf
